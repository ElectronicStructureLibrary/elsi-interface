# Please set the variables below according to your system!

# ========================================================
# Choose ELPA2 kernels.
# Possible choices include:
# Generic
#   - Should work for every platform
# SSE
#   - SSE instruction set
# AVX
#   - AVX instruction set
# AVX2
#   - AVX2 instruction set
# AVX512 (not yet functional)
#   - AVX512 instruction set
# BGQ
#   - IBM Blue Gene Q
# GPU
#   - NVidia CUDA

ARCHITECTURE = Generic

# For AVX/AVX2/AVX512, set the following variables.
# BLOCK_REAL can be 2,4,6; BLOCK_COMPLEX can be 1,2.
# (VY: add instruction!!)

# BLOCK_REAL = 2
# BLOCK_COMPLEX = 1

# For GPU, set the installation path of CUDA.
# CUDA = /opt/cuda/7.0

# ========================================================
# Switch on/off OpenMP.
# Set OPENMP to yes/no.

OPENMP = no

# ========================================================
# Set compilers and flags.
# Only Fortran compiler and flags are necessary,
# unless using AVX/AVX2 kernels and/or OpenMP.

MPIFC   = mpiifort
FFLAGS_E = -O3
MPICC   = mpicc
CFLAGS_E  = -O3 -mavx -funsafe-loop-optimizations \
          -funsafe-math-optimizations -ftree-vect-loop-version \
          -ftree-vectorize

# Set XLF to yes if using IBM XF compilers.
XLF = no

# ========================================================
# Installation paths of BLACS and ScaLAPACK.

SCALAPACK_LDFLAGS = -L/opt/intel/mkl/lib/intel64 -lmkl_scalapack_lp64 -lmkl_blacs_intelmpi_lp64 \
                    -lmkl_intel_lp64 -lmkl_sequential -lmkl_core -lpthread -lm
SCALAPACK_FCFLAGS = -I/opt/intel/mkl/include/intel64/lp64

# ========================================================
# Archive tools.

ARCH = ar
ARCHFLAGS = cr
RANLIB = ranlib
