include make.sys
export

all: elpa omm pexsi elsi

elpa:
	@echo ==========================
	@echo = Start building ELPA... =
	@echo ==========================
	cd $(THIS_DIR)/external/ELPA && ${MAKE} -f Makefile.elsi; ${MAKE} -f Makefile.elsi install
	@echo ===================
	@echo = ELPA installed. =
	@echo ===================

omm:
	@echo ============================
	@echo = Start building libOMM... =
	@echo ============================
	cd $(THIS_DIR)/external/libOMM && ${MAKE} -f Makefile.elsi
	@echo =====================
	@echo = libOMM installed. =
	@echo =====================

pexsi:  
	@echo ===========================
	@echo = Start building PEXSI... =
	@echo ===========================
	cd $(THIS_DIR)/external/PEXSI && mkdir -p lib
	cd $(THIS_DIR)/external/PEXSI/src && ${MAKE} -f Makefile.elsi 
	-cp $(THIS_DIR)/external/PEXSI/src/f_ppexsi_interface.mod $(THIS_DIR)/external/PEXSI/include
	cd $(THIS_DIR)/external/PEXSI/fortran && ${MAKE} -f Makefile.elsi
	@echo ====================
	@echo = PEXSI installed. =
	@echo ====================

elsi:
	@echo ==========================
	@echo = Start building ELSI... =
	@echo ==========================
	cd ELSI && ${MAKE} -f Makefile.elsi
	@echo ===============================
	@echo = ELSI compiled successfully. =
	@echo ===============================

install:
	cd ELSI && ${MAKE} -f Makefile.elsi install
	@echo ======================================
	@echo = ELSI library created successfully. =
	@echo ======================================

check:
	cd ELSI && ${MAKE} -f Makefile.elsi check

clean: cleanelpa cleanomm cleanpexsi cleanelsi

cleanelsi:
	cd ELSI && ${MAKE} -f Makefile.elsi clean

cleanelpa:
	cd $(THIS_DIR)/external/ELPA && ${MAKE} -f Makefile.elsi clean

cleanomm:
	cd $(THIS_DIR)/external/libOMM && ${MAKE} -f Makefile.elsi clean

cleanpexsi:
	cd $(THIS_DIR)/external/PEXSI/src && ${MAKE} -f Makefile.elsi cleanall
	cd $(THIS_DIR)/external/PEXSI/fortran && ${MAKE} -f Makefile.elsi cleanall
	rm -f $(THIS_DIR)/external/PEXSI/include/f_ppexsi_interface.mod
	rm -f $(THIS_DIR)/external/PEXSI/src/f_ppexsi_interface.mod
